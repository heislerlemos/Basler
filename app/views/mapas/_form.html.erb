<%= form_with(model: mapa, local: true) do |form| %>
  <% if mapa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mapa.errors.count, "error") %> prohibited this mapa from being saved:</h2>

      <ul>
      <% mapa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :latitude %>
    <%= form.text_field :latitude, id: :mapa_latitude %>
  </div>

  <div class="field">
    <%= form.label :longitude %>
    <%= form.text_field :longitude, id: :mapa_longitude %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :mapa_address %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :mapa_description %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :mapa_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
