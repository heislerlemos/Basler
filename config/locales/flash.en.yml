en:
  flash_messages:
    #
    # These are the translations for the different headings.
    #
    headings:
      success: "Obrigado por enviar a mensagem. Entraremos em contacto consigo em breve"
      info: "Info!"
      warning: "Warning!"
      error: "Não foi possivel enviar a mensagem."
    #
    # Add default flash messages:
    # defaults:
    #   success: "This is a notification"
    #   error: "Something went wrong"
    #   create:
    #     success: "Successfully created!"
    #     error: "Something went wrong. Please take a look at the form to see what went wrong."
    #
    # Add flash messages for controller actions:
    # products:
    #   create:
    #     success: "This is a notification"
    #     error: "Something went wrong"
    #
    # Inside controller:
    # def create
    #   flash!(:success, :error)
    #   redirect_to(:root)
    # end
    #
    # OR
    # 
    # def create
    #   redirect_to(:root, :flash => [:success, :error])
    # end
    #
    # OR
    #
    # def create
    #   flash_now!(:success, :error)
    #   render("new")
    # end